[packages]
java = "11"

[build]
cmd = "./mvnw -DoutputFile=target/mvn-dependency-list.log -B -DskipTests clean dependency:list install -Pproduction"

[deploy]
cmd = "java -jar target/*.jar --server.port=$PORT"
